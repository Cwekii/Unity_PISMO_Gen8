Cwekii 091 921 3062
Tina Novak tnovak@rk-smz.hr 099 218 4377

Matematički zapis

5 + 8 = 13

Programerski zapis

13 = 5 + 8


1.)
x + 8 = 13
x = 13 - 8
x = 5

13 = 8 + x

- varijabla - mjesto u memoriji rezervirano za pohranu podataka ovisno o tipu podatka

- INT (int, integer) - za cijele brojeve
(8, 100, 253, 69420, -2, -911, 0)

npr.
int x = 3
int y = -2
int z = 0

1.) Koliki je Z ako se u njega sprema zbroj x i y?

Z = x + y

2.) Napišite 3 varijable vaših naziva koje su sve sa cijelim brojevima. Sve 3 varijable se zbrajaju i zapisuju u četvrtu varijablu naziva "rezultat"

int x = 2
int y = 3
int a = 5
int rezultat = 0

rezultat = x + y + a
-----------------------------
int vino = 9
int voda = 7
int gemišt = -10
int rezultat = 0

rezultat = vino + voda + gemišt

- FLOAT - float - decimalni brojevi
(5.3, 6.9, 4.5713412, -3.4, -2.0, 0.0, 0.23)
	- kada se koristi float obavezno je staviti f na kraj broja
	- npr. 5.3f, -24.56f, 0f, 22.1f, 6.0f, 6f, .25f, 0.25f

3.) imate 3 varijable decimalnih brojeva, "mama", "tata" i "beba". Neka se u bebu sprema zbroj vrijednosti mame i tate


float mama = 1f (1.0f)
float tata = 2.5f
float beba = 0f (0.0f)

beba = mama + tata

4.) Napišite 3 varijable float vrijednosti gdje se od prve oduzima zbroj druge i treće. Rezultat se zapisuje u prvu varijablu.

float zagreb = 3.0f
float kutina = 2.8f
float davor = 0.842f

riješenje 1.
zagreb = zagreb - (kutina + davor)

riješenje 2.
zagreb = zagreb - kutina - davor

riješenje 3.
zagreb -= kutina + davor

5.) Koliki je zbroj X i Y ako znamo da je:
float X = 4.55f
float Y = 69f
float XY = 0f

XY = X + Y

6.) zapišite programski sljedeći zapis:
weed + mars - uho = droga
uho * 2.5 + pussy = slon
slon * droga = rezultat

int weed = 420
int mars = 50
int uho = 1337
int pussy = 1

int droga = 0
float slon = 0f
float rezultat = 0f

droga = weed + mars - uho
slon = uho * 2.5f + pussy
rezultat = slon * droga

7. kolika je razlika x i y ako je x 19, a y je 7.5. Rezultat zapišite u x

float x = 19f
float y = 7.5f

x = x - y || x -= y

8.) Zbrojite x i y ako je x 5 a y je 8.2, a sprema se u y

int x = 5
float y = 8.2f

y = x + y || y += x

9. Programski zapišite matematički zapis: 9 + 11 = 20
Obavezno definirajte i imenujte varijable

int a = 9
int b = 11
int c = 20 || 0

c = a + b

10.) Početna vrijednost healtha je 100, primamo damage u vrijednosti 17.6, kolika je konačna vrijednost healtha?

float health = 100f
float damage = 17.6f

health -= damage || health = health - damage

11.) Pretvorite u programski zapis sa vašim vrijednostima
susjed + mama - tata * krevet = problem

int susjed = 6
int mama = 9
int tata = 420
int krevet = 1
int problem = 0

problem = susjed + mama - tata * krevet

12.) Pretvorite u programski zapis sa vašim vrijednostima
bomba * 2 + teroristi - majmun / 3.2 = wtf

int bomba = 1
int teroristi = 22
int majmun = 11
float wtf = 0f

wtf = bomba * 2 + teroristi - majmun / 3.2f

13. Na početku vaš lik ima 52 mane, iskoristi ability koji košta 4.5 mana i potom ste ga iskoristili još 3 puta. Nakon svega popijete potion koji vam da 10 mana. Koliko imate mane? Napišite programski.

float mana = 52f
float ability = 4.5f
int potion = 10

mana -= ability
mana -= ability * 3
mana += potion

- Točka zarez ; se piše na kraju linije naredbe

float mama = 56.4f; 
int konj = 3; 
float rez = 0f;

rez = mama + konj;


- Što je to "null", a što je to "0"?
int rola = 0;
int rola;

int rolaPapira = 4;
maknemo li role onda bude
rolaPapira = 0;
ili
rolaPapira;

14.) Ušli ste u borbu. U šanžeru imate 15 metaka od maksimalnih 20. Ispucali ste ih 4, te potom reloadali šanžer. Napišite to programski, obavezno korak po korak.

int šanžer = 15;
int maxŠanžer = 20;

šanžer -= 4;
šanžer = maxŠanžer;

15.) U varijablu "students" spremajte količinu učenika u učioni. Učiona je na početku prazna i ne zna se koliko učenika može biti. Prvo ih dođe 20, nakon toga ode ih 5, zatim dođe 4, te potom ih ode 12 i na zadnjem satu se broj učenika udvostruči.
Napišite programski i obavezno korak po korak.

int students;

students += 20; //20
students -= 5; //15
students += 4; //19
students -= 12; //7
students *= 2; //14

16.) Na početku prvi igrač ima 12 ponija, drugi igrač ima 10 ponija. Koliko ponija ima koji igrač ako prvi otme drugome 4 ponija. Napišite programski.

int playerOne = 12;
int playerTwo = 10;

playerOne += 4;
playerTwo -= 4;

17.) Zbrojite x koji je 17.2 i y koji je 12.32. Rezultat napišite u varijabli "Maska"

float x = 17.2f;
float y = 12.32f;
float Maska;

Maska = x + y;

18.) Povećajte vrijednost x za vrijednost x, ako je x 5.

int x = 5;

x += x;

19.) Neka je x vrijednost 6, a y je 7. Njihov rezultat zbrajanja uvećajte (množenje) za vrijednost y i spremite u z

int x = 6;
int y = 7;
int z;

z = (x + y) * y;

20.) Igrač ima 100 HP-a, pao je sa visine 8 metara i izgubio 20% HP-a. Zatim mu se HP regenerirao za 8 (brojčano) i potom je pokušao ponovo skočiti, ali je sada pao sa visine 7 metara i izgubio 15.7% HP-a. Napišite ovo programski. (Postotci se odnose na trenutni HP, a ne na maksimalni)

float hp = 100f;

hp = hp - (hp * 0.2f); || hp -= hp * 0.2f;
hp += 8; || hp = hp + 8;
hp = hp - (hp * 0.157f); || hp -= hp * 0.157f; || hp *= 0.843f;

21.) Na početku je z 12 i prvo ga povećamo za 3, a zatim ga umanjimo za 2%. Napišite ovo programski.

float z = 12f;

z += 3;
z -= z * 0.02f; || z *= 0.98f;

22.) Imate 10 pokrajina. Svaka pokrajina po potezu daje 1 zlatnik. Nakon 3 poteza si izgubio 50% pokrajina. Prošlo je još 2 poteza. Koliko imaš zlatnika na kraju? Napiši programski. Napiši brojevima koliko imaš zlatnika na kraju.

int pokrajina = 10;
int zlatnika;

zlatnika += pokrajina * 3 * 1; //nakon 3 poteza za svaku pokrajinu po jedan zlatnik i sada imamo 30 zlatnika
pokrajina = (int)(pokrajina * 0.5f);
zlatnik += pokrajina * 2 * 1; //nakon 2 poteza za svaku pokrajinu po jedan zlatnik, 40 zlatnika imamo ukupno

- kada ispred neke matematičke naredbe ili broja stavite (int) rezultat će se spremati u intu tako što će zanemariti float vrijednost, dakle NEĆE zaokružiti, već će pročitati SAMO CIJELI broj

23.) Na početku je bilo 100 navijača, od njih je 13 donjelo baklje (njih nisu pustili). Redari nisu pustili na utakmicu svakog drugog navijača. Koliko je navijača ušlo na stadion? Napiši programski.

int navijači = 100;
int hooligani = 13;

navijači -= hooligani; //87
navijači -= (int)(navijači / 2); || (int)(navijači * 0.5f);


24.) Igrač ima 250 kristala, dao je 80 kristala čarobnjaku. Potom je prošao level i zaradio 70 kristala. Koliko igrač ima kristala, a koliko čarobnjak ima kristala? Riješite programski i odgovorite riječima.

int kIgrač = 250;
int kČarobnjak;

kIgrač -= 80;
kČarobnjak += 80;
kIgrač += 70;

Igrač ima 240 kristala, a čarobnjak ima minimalno 80 kristala

25.) Baka je dedi dala 12 kuna (za pivu), a deda je već imao 12 kuna, došla je vrana i uzela baki 8 jabuka, a baka je imala ukupno 10 jabuka. Koliko ce deda popiti piva ako je jedna piva 6 kn, a koliko baka ima jabuka? Riješite programski i odgovorite riječima.

int baka = 12;
int deda = 12;
int jabuke = 10;
int cijenaPive = 6;
int popijenePive;

deda += baka;
popijenePive = (int)(deda / cijenaPive);
jabuke -= 8; 

Deda će popiti 4 pive, a baka ima 2 jabuke

26.) Max inventory je 100, trenutno je pun 67, igrač izbaci 13 nepotrebnih itema. Potom ubije trolai uzme 45 itema. Nakon što je pregledao loot, baci 33 itema. Koliko je pun inventory na kraju, ako svaki item zauzima jedno mjesto? Napišite programski.

int iMax = 100;
int i = 67;

i -= 13; //54
i += 45; //99
i -= 33; //66

27.) Max HP je 100, body shot je 20 damage. Vaš lik ima 80 HP na početku borbe i odmah te neprijatelj pogodi "body shot". Potom padneš i izgubiš 2% trenutnog HP-a i usput te neki NPC nabije i napravi ti damage u iznosu od 8% max HP-a. Regenerira ti se HP na max, ali te opet neprijatelj pogodi "body shot" i head shot (head shot je duplo veći od body shota). 
Jesi li živ na kraju? Napiši programski.

float maxHP = 100f;
int bodyShot = 20;
float hP = 80f;
float headShot;

headShot = bodyShot * 2;
hp -= bodyShot; // 60
hp -= 0.02f * hp; //58,8
hp -= 0.08f * maxHP; //50,8
hp = maxHP; //100
hp -= bodyShot; //80
hp -= headShot; //40 hp ostalo, živi smo!


DZ 1

28.) Igrač pero ima 4 zlatne lopte, a igrač marko ima 6 zlatnih lopti, igrač pero je marku oteo jednu zlatnu loptu, te mu se poslje marko nasao sa sestrom doma i usput vratio svoju loptu i ukrao dvije perine.
Koliko lopti ima na kraju Marko, koliko Pero i čija sestra je izgubila nevinost?

int pero = 4;
int marko = 6;

marko -= 1;
pero += 1;
pero -= 3;
marko += 3;
---------------------------------------------

marko--;
pero++;
pero -= 3;
marko += 3;

29.) Igrač ima 20 mana. Iskoristio je vatrenu magiju koja vrijedi 5.5 mana, a nakon toga uzastopno dvije ledene magije koje vrijede 3 mane. Koliko mu je mana ostalo?
Napiši programski.

float playerOne = 20f;
float fireMgc = 5.5f;
int iceMgc = 3;

playerOne -= fireMgc;
playerOne -= iceMgc * 2;

//ostalo mu je 8.5 mane

30.) Na prvenstvu se natječu Nizozemska, Hrvatska, Španjolska i Italija. Svaka pobjeda nosi 2 boda.  Hr je pobjedila Nizozemsku, Italiju i Španjolsku. Te nakon utakmice sa Španjolskom zbog pijanstva dobila 2 kaznena boda. Koliko bodova ima Hrvatska.

int hrv;
int bod = 2;
int brojUtakmica = 3;
int kazenBod = 2;

hrv = brojUtakmica * bod; // 6
hrv -= kazenBod; // 4

31.) Saša je imao 10 lopti. Dvije je dao psu. Pas je jednu pojeo, koliko je lotpi ostalo psu?

int sasaLopte = 10;
int pasLopte;

sasaLopte -= 2;
pasLopte += 2;
pasLopte--;

32.) Mini ima tank od 60L. Do Novske potroši 20L, cestarina je u jednom smjeru 40 kn. Koliko će novaca izkamatariti tjedno ako putujemo u oba smjera autoputom i cijena goriva je 9 kn/L.

int miniTank = 60;
int trosi = 20;
int cjCestarine = 40;
int cjGoriva = 9;
int ukupnoGorivo;
int ukupnoNovaca;
int ukupnoCestarine;

ukupnoCestarine = (cjCestarine * 2) * 7; //560
ukupnoGorivo = 2 * trosi * 7; // 280
ukupnoNovaca = ukupnoCestarine + ukupnoGorivo * cjGoriva; //3080 kn tjedno
--------------------------------------------
int t = 7; //tjedan dana
int c = 40; //cestarina u jednom smjeru
int p = 20; //potrosnja u jednom smjeru
int cg = 9; //cijena goriva
int n; //konacna cijena

n = t * ((2 * p * cg) + 2 * c);

33.) Krešo ima 10 jabuka. Ivan mu kriomice pojede 2 jabuke. Koliko je jabuka pojeo Denis, a koliko je ostalo Kreši?

int kreso = 10;
int ivan;
int denis;

kreso -= 2;

//Kreso ima 8, denis ne znamo, ali 0

34.) Igrač ima 50 HP i 30 mana. Zatim na protivnika baci spell koji troši 3.5 mane. A istovremeno ga pogađa protivnikov spell koji radi 20 dmg. Istovremeno nosis shield koji blokira 50% dmg od tog spella. Koliko HP i mane imaš?

float hp = 50f;
float mana = 30f;
int spell = 20;
float shiled = 0.5f;
float spellMana = 3.5;

mana -= 3.5f;
hp -= spell * 0.5f;

// hp imamo 40, mane imamo 26.5


** PISANJE KOMENTARA **
- obavezno
- koristi se za druge ljude kada čitaju vaš kod
- programeri koji ne koriste komentare kažu "Prvo smo Bog i ja znali kako radi ovaj kod, sada zna samo Bog"
- komentari se pišu sa "//" i pišete ih iznad ili u redu narebde/linije/metode na koju se odnose

npr.
int health = 100; //varijabla za health od playera
int dmg = 25; //varijabla za damage od zombija
int dmg2 = 30; //vaijabla za damage od vampira

npr. Komentiranje više redova - koristi se kada kod radi, ali se našao/traži bolji način pa ga se zakomentira da se ne odvija u programu

/*
float bana = 89f;

bana = bana * 2 - 45 + 3.9f;
*/

35. Iskomentirajte i riješite zadatak:
- Max HP je 100, body shoot je 20 damage. Vaš lik ima 89 HP na početku borbe i odmah te neprijatelj pogodi "body shoot". Potom padneš i izgubiš 2% HP-a i usput te neki NPC nabije i napraviti damage u iznosu od 8% maxHP-a. Regenerira ti se HP na max, ali te opet neprijatelj pogodi "body shoot" i head shoot koji je duplo veći od body shoota. Potom je NPC se zbugao i nanio ti istu štetu kao i prije.

int maxHP = 100; //maksimalni iznos healtha(zdravlja) od igrača
int bodyShot = 20; //iznos damagea (štete) koje nanosi neprijatelj u body
float hp = 89f; //iznos trenutnog healtha (zdravlja)

hp -=bodyShot; //igrač je primio bodyshot i pao je na 69hp-a
hp *= 0.98f; //igrač pada i prima štetu 2%
hp -= 0.08f * maxHP; //NPC nanosi štetu
hp = maxHP; //igrač regenerira svoj hp
hp -= bodyShot; //igrač je primio bodyshot i pao je na 80hp-a
hp -= bodyShot * 2; //igrač je primio headshot i pao je na 40hp-a
hp -= 0.08f * maxHP; //NPC nanosi štetu i sada smo na 32 hp

36.) Iskomentirajte i riješite zadatak: Pri rezultatu 100/90 domacini su dali jos 3 kosa do kraja utakmice. A gosti su dali 5 koseva. Koliki je bio rezultat na kraju utakmice ako znam da jedan koš vrijedi 2 boda.

int d = 100; // Početni rezultat domacina
int g = 90; // Pocetni rezultat gosti
int k = 2; // Broj bodova koji svaki kos donosi

int rd; // Konacni rezultat domacina
int rg; // Konacni rezultat gosti

rd = d + 3 * k; // Broj koseva koje su postigli domacini
rg = g + 5 * k; // Broj koseva koje su postigli gosti

37.) Iskomentirajte i riješite zadatak: Cwekii ima 20 debila na unity edukacije od kojih mora napraviti game developera, 5 debila odustaje od edukacije, 3 debila se prebacuju sa blendera, ali od pridošlica samo dvojica završavaju. Koliko je game developera Cwekii napravio?

int debili = 20; //početni broj polaznika unity edukacije

debili -= 5; //otišlo je pet debila sa edukacije
debili += 3; //došlo je troje debila sa Blender edukacije
debili --; //broj polaznika se smanjio jer jedna od pridošlica nije završila edukaciju
//17 debila je završilo edukaciju i postalo game developerima

38.) Iskomentirajte i riješite zadatak: Igrac ima 100 max hp i 50 movement pointsa, također ima 1 potion koji ga heala 10% max HP. Ima dvije vrste napada specijalni koji radi 50 dmg i troši 30 movement pointa i obicni napad koji radi 30 dmg i ne kosta nista. Monster ima 126 HP-a i tuce za 30 dmg. Tuku se naizmjenicno svako jedan napad po krugu, a potion ne troši krug i igrač napada prvi. Combat je gotov kada igrač ili monster umru. Tko umre prvi ako igrač koristi sve što mu je na raspolaganju?




**UVJETI**
Što je uvjet?
- Uvjet je skup riječi koje govore što se treba ispuniti kako bi se izvršio niz/red naredbi
- Primjeri uvjeta:
	- AKO - if
	- DOK - while
	- Kada - when (ne postoji)
	- ZA - for
- Kako se zadovoljava uvjet? Sa provjerom znakova vrijednosti:
== jednako je - provjera jesu li vrijednosti jednake
= dodjeljuje vrijednost
!= različito je
|| (alt gr + W) ili - ili je jedna vrijednost točna ili je druga
&& i - obje vrijednosti moraju biti točne/istinite
> veće od
< manje od
<= manje ili jednako od
>= veće ili jednako od

primjeri if petlji:
if(1 +1 == 2) --> istina
if(2 + 3 != 4) --> istina
if(18 > 19) --> laž
if(21 <= 21) --> istina

kao da piše:
istina -> 1 + 1 = 2
laž -> 5 + 5 = 11
if(istina || laž)
if(1 + 1 == 2 || 5 + 5 == 11) ->istina
if(istina || laž) -> istina
if(istina || istina) -> istina
if(laž || istina) -> istina
if(laž || laž) -> laž
if(istina && istina) -> istina
if(istina && laž) -> laž
if((laž && laž) || istina) -> istina

39.) x je veličine 8, a y je veličine 13, ako je njihov zbroj manji od 50 neka se x poveća za samoga sebe, a ako je zbroj veći ili jednak 50 neka se y poveća za samog sebe, a u svim ostalim slučajevima x se treba pomožiti za y i spremiti u x.

int x = 8;
int y = 13;

if(x + y < 50)
{
	x += x;
}

else if(x + y >= 50)
{
	y += y;
}

else
{
	x *= y;
}

40.) Vrijednost x je -3.6, a vrijednost y je 10. Ako je njihov umnožak veći od 100 neka se x uveća za taj umnožak, ako je manji od 100 neka se y uveća za taj umnožak, a ako je umnožak jednak 100 neka se x i y uvećaju za sami sebe.

float x = -3.6f;
flaot y = 10f;

if(x * y > 100)
{
	x += x * y;
}

else if(x * y < 100)
{
	y += x * y;
}

else if(x * y == 100)
{
	x += x;
	y += y;
}

41.) Igračev health je 30. Regeneracija ce mu krenuti ako mu je health pao ispod 20 i povećati će se za 12. 

int hp = 30;
int regen = 12;

if(hp < 20)
{
	hp += regen;
}

42.) Igračev health je 31. Regeneracija ce mu krenuti ako mu je health pao ispod 20 i povećati će se za 12. Igrac prvo prima dmg od 9. Zatim je popio potion koji mu je vratio 10 hp i potom je primio critical strike koji mu je skinuo 26 hp. Pitanje je jel se igracu dogodio health regen i ako je kada? Napisati programski i oznacite komentarima kada se dogodila regeneracija.

int hp = 31;
int dmg = 9;
int strike = 26;
int potion = 10;
int regen = 12;

if(hp < 20)
{
	hp += regen;
}

hp -= dmg; //22
//provjera ide li regeneracija
hp += potion; //32
//provjera ide li regeneracija
hp -= strike; //6
//provjera ide li regeneracija
//Ide regeneracija


43.) Igrac jedan i dva su imali 100 hp. Igrac dva je udario igraca jedan te mu oduzeo 25 hp, igrac jedan kontra napadom oštečuje igrača dva za 14 hp, te regenerira sebe za 10 hp. Koliko koji igrac ima hp?

int p1 = 100;
int p2 = 100;

p1 -= 25;
p2 -= 14;
p1 += 10;
//player one ima 85 hp, a player two ima 86 hp-a

44.) Pokemon evoluira kada dođe na 16 level. Za doći na 16 level potrebno mu je ukupno 4561 XP. Početni level mu je 1 i ima 0 xp. Odradio je 25 borbi gdje je dobio 25 xp po borbi i potom još 25 borbi gdje je u svakoj dobio 93 xp. Napravite provjeru imali dovoljno xp za evoluciju, ako nema gubi sav xp, a ako ima onda mu je level 16.

int pLvl = 1;
int xp = 0;
int evolvLvl = 16;
int evolvXP = 4561;
int fightOne = 25;
int fightTwo = 93;

xp += 25 * fightOne; //625
xp += 25 * fightTwo; //2325 -> 2950

if(xp >= evolvXP)
{
	pLvl = 16;
}

else
{
	xp = 0;
}


45.) Kapetan Miki ima 10 HP, ako primi damage veći od 5, a da ga to neće ubiti onda se Miki naljuti i njegov HP bude 20.

int mikiHp = 10;
int dmg;

if(dmg > 5 && mikiHp > dmg)
{
	mikiHp = 20;
}

46.) A je jacine 5, a B je jacine 17 ako je njihova razlika veca od 10 neka se broj A poveca za B, a ako je manja ili jednaka neka se B poveca za A

int A = 5;
int B = 17;

if(A - B > 10 || B - A > 10)
{
	A += B;
}

else if(A - B <= 10 || B - A <= 10)
{
	B += A;
}

47.) Imate varijablu X. Ako je X veći od 10, neka se X uveća za 20, a ako je X manji od 10 neka se umanji za 9. U ostalim slučajevima neka se X pomnoži sam sa sobom.

int X;

if(X > 10)
{
	X += 20;
}

else if(X < 10)
{
	X -= 9;
}

else
{
	X *= X;
}

48.) U šumi je 6 elfova, 5 gnomova i 10 orkova, ako je zbroj elfova i gnomova veći ili jednak orkovima, broj elfova se uveća za njihov broj (poduplaju se), a ako je njihov zbroj manji, orkovi pojedu 50% gnomova i 20% elfova. NAPOMENA, ne može se pojesti decimalno elfova niti gnomova.

int elfs = 6;
int gnoms = 5;
int orcs = 10;

if(elfs + gnoms >= orcs)
{
	elfs *= 2;
}

else if(elfs + gnoms < orcs)
{
	gnoms = (int)(gnoms * 0.5f);
	elfs = (int)(elfs * 0.8f);
}

49.) Pocetna vrijednost varijable je 830, X je 69, a Y je 538. ukoliko je umnožak x i y manji od 1000 i varijabla vrijednosti 830 zbrojena sa y je veca od 1000 , neka se varijabla sa pocetnom vrijednoscu 830 smanji za 100. Ukoliko je umnozak veci (samo to je uvijet), neka se pocetna varijabla poveca za 100. U svim ostalim slucajevima neka se pocetna varijabla pomnozi za 100.

int pocVar = 830;
int x = 69;
int y = 538;

if(x * y < 1000 && pocVar + y > 1000)
{
	pocVar -= 100;
}

else if(x * y < 1000)
{
	pocVar += 100;
}

else
{
	pocVar *= 100;
}

50.) Super Mario je imao 3 života na početku. Izgubio je 2. Dobio je jedan život tokom nivoa i dobio jedan nakon što je prešao nivo. Napiši programski.



51.) X je 15, Y je 100, ako je X izmedu 13 i 19 (ukljceni su i 13 i 19) povecaj Y za 20%. A ako nije onda Y smanji za 13%. Ne smiju se spremati vrijednosti u decimalnom zapisu.


52.) Cilj igre je skupiti 4 objekta, prsten, pehar, zlatnik i dijamant. Sa svakim skupljenim itemom aktivira se jedan boss, a kada se skupi svih 4 itema, igra je pređena. Odmah skupljate prsten, te dalje tim redosljedom. Sa koliko bossova ste se borili dok niste prešli igru?

53.) Vrijednost "nice" je 69, a vrijednost "blaze" je 420, ako je zbroj tih vrijednosti pomnožen sa 2 veći od 1300 neka se nice podupla, a ako je manji ili jednak 1250 neka se blaze podupla.

54.) Programski zapisi 11.79 + 9.01 = 19.80;



** VAR varijabla

var:
- vrsta varijable koja se prilagođava unosu
- ne može se definirati u početku
- koristi se samo unutar neke petlje/metode za privremenu vrijednost

npr.
Neka se rezultat množenja x i y poveća za 10 ako je x veći od 5 i spremite u varijablu rez, ali to odradite u 2 koraka

float x = 10.5f;
int y = 77;
float rez;

rez = x * y;

if(x > 5)
{
	var privremeno = x * y;
	rez = privremeno + 10;
}


55.) Igrac HP-a 100 je primio dmg 23. Popijemo potion koji nam vrati 15 hp. Napisi programski.

int pHP = 100;
int potion = 15;
int dmg = 23;

pHP -= dmg;
pHP += potion;

56.) Vi imate 15€, a vaš frend 25€ i vaš treći frend ima 10.5€. Vi takvi siromasi se odlučite kupiti Bugatti Vayrona koji košta 2500000€. Ako osoba koja ima najviše novaca ima duplo više novaca od osobe koja ima najmanje novaca, onda on plaća pola cijene. A ostala dvojica djele drugu polovicu po pola. Ako to nije slučaj onda svi plačaju jednak iznos. Napisite koliko svakome pojedinacno jos fali novaca.


float me = 15f;
float friendOne = 25f;
float friendTwo = 10.5f;
int buggattii = 2500000;

if(me > friendOne && me > friendTwo)
{
	if(friendOne < friendTwo)
	{
		if(me > friendOne * 2)
		{
			me -= buggattii * 0.5f;
			friendOne -= buggattii * 0.25f;
			friendTwo -= buggattii * 0.25f;
		}
		else
		{
			me -= buggattii / 3;
			friendOne -= buggattii / 3;
			friendTwo -= buggattii / 3;
		}
	}
	
	else if(friendOne > friendTwo)
	{
		if(me > friendTwo * 2)
		{
			me -= buggattii * 0.5f;
			friendOne -= buggattii * 0.25f;
			friendTwo -= buggattii * 0.25f;
		}
		else
		{
			me -= buggattii / 3;
			friendOne -= buggattii / 3;
			friendTwo -= buggattii / 3;
		}
	}
}

else if(friendOne > me && friendOne > friendTwo)
{
	if(me < friendTwo)
	{
		if(me * 2 < friendOne)
		{
			friendOne -= buggattii * 0.5f;
			me -= buggatti * 0.25f;
			friendTwo -= 0.25f;
		}
		else
		{
			me -= buggattii / 3;
			friendOne -= buggattii / 3;
			friendTwo -= buggattii / 3;
		}
	}
	else if(friendTwo < me)
	{
		if(friendTwo * 2 < friendOne)
		{
			friendOne -= buggattii * 0.5f;
			me -= buggatti * 0.25f;
			friendTwo -= 0.25f;
		}
		else
		{
			me -= buggattii / 3;
			friendOne -= buggattii / 3;
			friendTwo -= buggattii / 3;
		}
	}
}

else if(friendTwo > me && friendTwo > friendOne)
{
	if(me < friendOne)
	{
		if(me * 2 < friendTwo)
		{
			friendTwo -= buggattii * 0.5f;
			me -= buggatti * 0.25f;
			friendOne -= 0.25f;
		}
		else
		{
			me -= buggattii / 3;
			friendTwo -= buggattii / 3;
			friendOne -= buggattii / 3;
		}
	}
	else if(friendOne < me)
	{
		if(friendOne * 2 < friendTwo)
		{
			friendTwo -= buggattii * 0.5f;
			me -= buggatti * 0.25f;
			friendOne -= 0.25f;
		}
		else
		{
			me -= buggattii / 3;
			friendOne -= buggattii / 3;
			friendTwo -= buggattii / 3;
		}
	}
}

else
{
	me -= buggattii / 3;
	friendOne -= buggattii / 3;
	friendTwo -= buggattii / 3;
}

-------------------------------------------------------------------------------

float me = 15f;
float friendOne = 25f;
float friendTwo = 10.5f;
int buggattii = 2500000;

if(friendOne > 2 * me || friendOne > 2 * friendTwo)
{
	friendOne -= buggattii * 0.5f;
	me -= buggatti * 0.25f;
	friendTwo -= 0.25f;
}

else if(me > 2 * friendOne || me > 2 * friendTwo)
{
	me -= buggattii * 0.5f;
	friendOne -= buggatti * 0.25f;
	friendTwo -= 0.25f;
}

else if(friendTwo > 2 * friendOne || friendTwo > 2 * me)
{
	friendTwo -= buggattii * 0.5f;
	friendOne -= buggatti * 0.25f;
	me -= 0.25f;
}

else
{
	me -= buggattii * 0.33f;
	friendOne -= buggattii * 0.33f;
	friendTwo -= buggattii * 0.33f;
}


57.) x je 50, y je 100, ako je njihov zboj veci od trostruke vrijednosti x onda neka se x podupla, a ako je manji neka se y udvostruci, a ako su jednaki neka se oba kvadriraju.

int x=50;
int y=100;

if(x+y>x*3)
{
	x*=2;
}
else if(x+y<x*3)
{
	y*=2;
}
else if(x+y==x*3)
{
	x*=x;
	y*=y;
}

58.) igrac ima 100 HP. Ako mu HP padne ispod 75 regenrira mu se health za 10, a ako padne ispod 25 i igrac ostane ziv, regenerira mu se za 25 HP. Igrac prvo primi dmg od 55, a zatim opet primi dmg od 55. Koliko hp ima igrac?

int hp=100;
int dmg=55;
if (hp<25 && hp>0)
{
	hp+=25;
}

else if (hp<75 && hp>0)
{
	hp+=10;
}


hp-=dmg; //100-55=45 
hp+=10; //45+10=55 hp je pao ispod 75 pa mu se dodaje 10
hp-=dmg; //55-55=0 hp pao ispod 25 pa mu se dodaje 25
hp+=25; //0+25=25

59.) Prvi vozac ima 10 kockica, a drugi ima 13. Kada se oni sudare onaj koji ima manje ukrade drugome jednu kockicu!


60.) Varijabla x je 15, varijabla y je dva puta veca od x. Ako je zbroj x i y manji od 50, x se poveca za y. Ako je zbroj x i y veci od 50 od x se oduzima y, a ako je zbroj jednak onda x dobiva y vrijednost.



*METODE*
- metoda je skup narebi koje se trebaju odraditi u određenim uvjetima i intervalu

Ključne metode u Unityu:

- Awake 
	- odvija se prije 0 framea
	- događa se prija stvaranje bilokakvih objekata na sceni
	- izvršava se samo jedan put

- Start
	- izvršava se netom prije prvog framea
	- izvršava se samo jedan put

- Update
	- svaki frame se jednom izvrši

kako se piše metoda:

void NazivMetode()
{
	linija naredbe;
}

* vidljivost metoda/varijabli
- public - ispred vrste varijable kojoj omogucavamo da mi kao developeri možemo unositi i mjenjati vrijednost varijable u Unity Inspectoru ili pozivati iz drugih skripti
npr. public float primjer = 4.7f;

- private - stavlje se ispred varijable i onemogucava vidjljivost i promjene vrijednosti varijable iz Unity Inspectora i pozivanje iz drugi skripti. Ako ispred varijable/metode ne piše ništa, automatski je private.
npr. private int primeric = 5; ili int primeric = 5;

*Debugiranje*
- postupak pružanja informacija developeru ima li greške i koja je greška ili ispis rada programa u konzoli

- Osnovo - Debug.Log("")
- Debug -> debugiraj (provjeri)
- Log -> ispiši u konzoli
- ("") - unutar navodnika tekst za ispis, poslje može varijabla za ispis (spajanje se radi sa pomoću znaka +)

npr 1.
public int x;
public int y;

void Start()
{
	var rez = x * y;
	Debug.Log("Rezultat je:" + rez);
}

npr 2.

void Start()
{
	Debug.Log("Odvila se start metoda");
}


Napomena za zadatke +61. ako ne piše da je javna varijabla onda je privatna

61. U startu neka se zbroje 2 javna broja, neka se u konzoli ispiše njihov zbroj.


public int x;
public int y;

void Start()
{
	var zbroj = x + y;
	Debug.Log(zbroj);
}

------------------------------------
public int x, y;

void Start()
{
	Debug.Log(x + y);
}

62.) Zbrojite u startu x koji je 17.2 i y koji je javan broj. Neka se rezultat sprema u varijablu "banana" i neka se ispiše u konzoli.

private float x = 17.2f;
public float y;
private float banana;

void Start()
{
	banana = x + y;
	Debug.Log(banana);
}

63.) Povecajte x koji je 5 za 5 svaki frame.

int x = 5;

void Update()
{
	x += 5;
}

64.) Neka je x 6, a y je 7 i njihov zbroj neka se izracuna u startu, a onda svaki frame neka se rezultat uveca za x

int x = 6;
int y = 7;
int z;

void Start()
{
	z = x + y;	
}

void Update()
{
	z += x;
}

65.) Neka je x 8, a y je javni broj i njihov zbroj neka se izracuna u startu, a onda svaki frame neka se rezultat poveca za polovicu x varijable.

int x = 8;
public float y;
float z;

void Start()
{
	z = x + y;
} 

void Update()
{
	z += x/2;
}


66.) Ako je vrijednost x -3.6, a vrijednost y je javna. Ako je zbroj veci od 20 neka se ispiše "To legendo", a ako je manji ili jednak neka se ispiše "Falia si budalo"

float x=-3.6f;
public int y;

void Start()
{
	if (x+y>20)
	{
		Debug.Log("To legendo");
	}
	else if (x+y<=20)
	{	
		Debug.Log("Falia si budalo");
	}
}



67.) U startu zbrojite dvije javne varijable od kojih se jedna zove "y". Neka se svaki frame x povecava za 5% iznosa y varijable. Svaki frame ponovo zbrojite x i y te ispišite vrijednost zbroja.

public float z;
public float y;
float x;

void Start()
{
	var zbroj;
	zbroj = z + y;
}

void Update()
{
	x += y * 0.05f;
	Debug.Log(x + y);
}

68.) Neka se u metodi start ispiše "Ja sam Start", u metodi awake "Ja sam mali Awake", te u konacnici neka se svaki frame ispisuje "Moje ime je medvjedić Lino".

void Start()
{
	Debug.Log("Ja sam Start");
}

void Awake()
{
	Debug.Log("Ja sam mali Awake");
}

void Update()
{
	Debug.Log("Moje ime je medvjedić Lino");
}

69.) Noice ;)

70.) Povecajte javnu varijablu X za jedan svaki frame. Neka se također svaki frame ispiše "Ja imam " + x + " godina".

 public int x;

void Update()
{
	x++;
	Debug.Log("Ja imam " + x + " godina");
}
------------------------------------------------------------------------------------------
*PONAVLJANJE*

1.) napišite primjer int varijable

int x; 

int sarma=5;
public int z = -69;

2.) napišite primjer float varijable

public float y = 13.57901f;
private float martin = 3.0f;

3.) Objasnite razliku između || (OR/ILI) i && (AND/I) sa primejrom if-else u start metodi

public int x;
public int y;

void Start()
{
	if(x>y || x==y) //x je 3 a y je 5
	{ 
		x += x;
	}

	else if(x < y && x <= 3)
	{
		x -= y;
	}
}

4.) Što znači !=

Različito vrijednost za uvjet

5.) x je 10, a y je 12. U Startu napravite provjeru ako su x i y jednaki neka se ispiše "Jednako je", ako su različiti neka se ispiše "Različito je"

int x = 10;
int y = 12;

void Start()
{
	if(x==y)
	{
		Debug.Log("Jednako je");
	}
	else 
	{
		Debug.Log("Različito je");+
	}
} 

6.) Ivica ima 5 jabuka, a Marica ima 2 jabuke, Ivica joj uzme 1 jabuku. Marica izjavi da je Ivica Smrad. Napišite programski

int Ivica=5;
int Marica=2;

void Awake()
{
	Marica-=1;
	Ivica+=1;
}

void Start()
{
	Debug.Log("Ivice smrad si");
}

7.) X je javni broj. Neka se prije nultog framea ispiše vrijednost x.

public int x;
void Awake()
{
	Debug.Log("Vrijednost od x je: " + x);
}

8.) U startu zbroji javnu vrijednost x i javnu vrijednost y. Ako je njihov zbroj 10, neka se svaki frame ispiše "Može", a ako njihov zbroj nije 10 neka se svaki frame ispiše "Reka san nemože!"

public int x;
public int y;
int zbr;

void Start()
{
	zbr = x + y;
}

void Update()
{
	if(zbr == 10)
	{
		Debug.Log("Može");
	}
	else
	{
		Debug.Log("Reka san nemože");
	}
}

----------------------------------------------------------------------------------------------


Nastavak:
71.) Imajte dva javna cijela broja. Njihov rezultat zbrajanja se sprema u javnu varijablu. Postupak je sljedeći. Svaki frame se prvi cijeli broj mora povecati za 1, potom se racuna rezultat zbrajanja prvog broja i drugog. Ako je rezultat manji od 500 ispisuje se ("Manji"), ako je između 500 i 2000 ispisuje se "Prosječan", a ako je veći ispisuje se "Največi"

72.) x ima vrijednost 7, ali je x privatna varijabla. Napišite da se u startu ispiše vrijednost x varijable.

73.) U početku zbroji javnu vrijednost koja prima vrijednosti cijelih brojeva i javnu vrijednost koja prima vrijednost decimalnih brojeva. Ako je njihov zbroj 10 neka se svaki frame ispiše "Banana", a ako je manji od 10 neka se ispiše "Mrkvica". Obavezno koristiti samo jednu metodu.

public int x;
public float y;

void Update()
{
	if(x + y == 10)
	{
		Debug.Log("Banana");
	}
	else if(x + y < 10)
	{
		Debug.Log("Mrkvica");
	}
}


74.) Napišite skriptu gdje se zbrajaju dva javna broja u metodi start i ispisuje im se rezultat na primjer: Rezultat zbrajanja brojeva 8 i 9.5 je 17.5


*Bool
- Što je bool?
- Bool je vrsta varijable koja pohranjuje u sebe isključivo 2 moguće vrijednosti, a to su točno ili netočno.
- može biti samo true ili false
- tehnički se može reći da ide 0 ili 1
- što ako je bool null vrijednosti -> false

npr.
bool banana = false;
bool died = true;
bool endGame; //kada ne piše vrijednost onda je null, ako je null to je kao da je 0, a pošto bool ne može biti broj, onda je false
bool zmjacek; <=> bool zmajcek = false;

npr. promjena boola sa "!"

bool magla = true;

void Start()
{
	!magla; //sada je bool magla promjenio vrijednost u obrnutu od koje je imao
	magla = true; //sada je bool magla true
	magla = false; //sada je bool magla false
}


75.) Neka se zbrajaju dva javna broja, ako je njihov zbroj veci od 15 neka bool endGame postane true, ako su manji ili jednaki neka je false. Ovo napravite u drugoj, vlastitoj metodi, Ako je endGame true onda neka se ispiše u konzoli da je igra završena, a ako je false neka ispiše da si preživio.

public int x;
public int y;
bool endGame = false; //bool endGame;

void Start()
{
	if(x + y > 15)
	{
		endGame = true;
	}
	else
	{
		endGame = false;
	}

	CheckIfEndGame();
}

void CheckIfEndGame()
{
	if(endGame == true) //if(endGame) -> kao da pita je li bool istinit
	{
		Debug.Log("Igra je završena");
	}
	else if(endGame == false) //if(!endGame) -> kao da pita je li bool lažan, dakle ne mjenja vrijednost
	{
		Debug.Log("Preživio si");
	}
}

76.) Neka su dva javna broja float vrijednosti. Za pocetne vrijednosti neka budu 12.3 i 18.2. Definirajte i privatni bool banana. Banana je na pocetku false. Ako je umnožak dva broja veci ili jednak 100 neka banana postane true i u konzoli neka se ispiše "Banana je pojedena", ako je banana false neka se ispiše "Banana je citava". 
Riješite na dva načina. 1. koristeći samo metodu Awake. 2. koristeći metodu start i vlastitu metodu.

77.) Napišite skriptu gdje ćete imati start metodu i metodu naziva vašeg imena. U startu neka se pozove metoda vašeg imena. Metoda vašeg imena neka ima naredbu da ispiše u konzoli "Ja sam unity developer i pametniji sam nego gobelodt developer".

78.) Napišite skriptu koja u sebi nosi 4 javna floata. U startu se radi provjera matematickog zapisa:
x + y * (a / b) - x / a * 18 * b - x + y = rez
Ako je rezultat veci od drugog zapisa:
x - y / (a * b) + x * a / 18 / b + x - y + 23 = rez
Onda neka se u updateu rezultat povecava za jedan i ispisuje "Ja sam rezultat: " + rezultat

public float x;
public float y;
public float a;
public float b;

float rez1;
float rez2;
bool jelUredu;


79.) Napišite skriptu gdje imate jedan bool i jedan javni broj. Ako je taj javni broj razlicit od 18 onda neka se pozove metoda MjenjajMe koja mjenja vrijednost tako da ako je bool bio true pretvara ga u false, a ko je bio false pretvara ga u true.

80.) Napišite skriptu koja množi dva javna broja. Ako je njihov umnožak različit od 100 ili je veci od 0 onda neka se pozove druga metoda naziva JaSamMetodaDva u kojoj ce se raditi djeljenje prvog broja sa drugim i ako je rezultat djeljenja manji od 100 neka se ispiše "Ja sam MVP" (Inače MVP je kratica za Most Valuable player), ako je bilokoje drugo riješenje neka se u konzoli ispiše "Ja sam N00b".

81.) Imate javni bool. Ako je taj bool jednak true u startu onda neka se ispiše "Volim mrkvu", a ako je različit od true onda neka se ispiše "Volim borovnicu".

82.) Sto ce se ispisati?

public int x = 10;
public int y = 3;

void Start()
{
	Debug.Log(x/y);
}

83.) Uvecaj int x za 1 svaki drugi frame.

84.) x i y su javni brojevi. Ako je njihov zbroj manji od 5 u konzoli ispiši "Mužu, prevarila sam te", a ako je veći ispiši "Dijete je tvoje!"



*NOVA PETLJA - For petlja*

for => dok je uvjet zadovoljen obavlja naredbe
int i => brojač, nemora se zvati i, ali se tako najčešće i po defultu zove
uvjet => uvjet koji mora biti zadovoljen
inkrement => utječe na brojac, naredba s kojom djelujemo na brojac prema više
dekrement => utječe na brojač, naredba s kojom djelujemo na brojač na manje

način pisanja for petlje:

for(brojac; uvjet; inkrement/dekrement)
{
	naredba;
}

primjer:

int brojEnemya = 3;

void Start()
{
	for(int i = 0; i < brojEnemya; i++)
	{
		Debug.Log("Stvoren je " + i + ". enemy");
	}
}

KRIVO:

//krivo postavljen brojač/uvjet
for(int i = 0; i > 5; i++)
{
	Debug.Log(i);
}

//infinity loop - krivi uvjet/dekrement/inkrement
for(int i = 9; i < 10; i--)
{
	Debug.Log(i);
}


85. Zadatak za for petlju
Napravite skriptu koja ima *maximalno* 3 if/else if petlji po metodi. Neka je svaki uvijet definiran bool-om.
Prvi bool se zove "easy" drugi se zove "normal", a treci "harderDaddy".
a) ako je svaki bool false na pocetku, neka bool "normal" postane true
b) svaki bool mora biti javan
c) za uvijet easy neka se broj enemy-a (varijabla enemies) stavi na 5 i neka joj je to pocetna vrijednost (varijable)
d) Ako je igra na normal onda neka je broj enemya dva puta veci od easy nacina
e) Ako je igra na harderDaddy onda neka je broj enemya tri puta veci nego u normal nacinu igre
f) Nakon što se provjeri koji je mod igre (težina, odnosno koji je bool tocan), neka se ispiše u konzoli svaki enemy posebno kao u primjeru: Debug.Log("Stvoren je " + i + ". enemy!") - npr. Stvoren je 3. enemy | Stvoren je 10. enemy
g) Kada su se ispisali svi enemy-i (prestala je for petlja iz zadataka) neka se ispiše poruka "Sretno ti u " + težina igre + " nacinu igre!" - npr. Sretno ti u easy nacinu igre!

public bool easy;
public bool normal;
public bool harderDaddy;
int enemies = 5;

void Start()
{
	if(!easy && !normal && !harderDaddy)
	{
		normal = true;
	}
	Spawn();
}

void Spawn()
{
	if(easy)
	{
		enemies = 5;
		for(int i = 1; i <= enemies; i++)
		{
			Debug.Log("Stvoren je " + i + ". enemy!");
		}
		Debug.Log("Sretno ti u easy nacinu igre!");
	}

	if(normal)
	{
		enemies *= 2;
		for(int i = 1; i <= enemies; i++)
		{
			Debug.Log("Stvoren je " + i + ". enemy!");
		}
		Debug.Log("Sretno ti u normal nacinu igre!");
	}

	if(harderDaddy)
	{
		enemies *= 6;
		for(int i = 1; i <= enemies; i++)
		{
			Debug.Log("Stvoren je " + i + ". enemy!");
		}
		Debug.Log("Sretno ti u harderDaddy nacinu igre!");
	}
}


----------------------Drugi način---------------------------------

public bool easy;
public bool normal;
public bool harderDaddy;
int enemies = 5;

void Start()
{
	if(easy)
	{
		Spawn();
		Debug.Log("Sretno ti u easy nacinu igre");
	}
	
	else if(harderDaddy)
	{
		enemies *= 6;
		Spawn();
		Debug.Log("Sretno ti u harderDaddy nacinu igre");
	}
	
	else
	{
		normal = true;
		enemies *= 2;
		Spawn();
		Debug.Log("Sretno ti u normal nacinu igre");
	}
}

void Spawn()
{
	for(int i = 1; i <= enemies; i++)
	{
		Debug.Log("Stvoren je " + i + ". enemy");
	}
}














